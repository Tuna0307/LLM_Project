# ── Environment (contains API keys — NEVER commit) ──
.env

# ── Python cache ──
__pycache__/
*.pyc
*.pyo
.venv/
venv/
env/

# ── Database files (regenerated locally) ──
db/chroma/
db/*.db
*.db

# ── Uploaded files (all types — too large / private for git) ──
data/raw/*
!data/raw/.gitkeep
data/processed/

# ── Reference papers (not source code) ──
# Only ignore PDFs in the project root, not requirements.txt or README
/*.pdf

# ── IDE ──
.vscode/
.idea/
*.code-workspace

# ── Frontend build / dependencies ──
node_modules/
frontend/dist/
frontend/.env

# ── OS ──
.DS_Store
Thumb.db
Thumbs.db

# ── Accidental output files ──
/-*
